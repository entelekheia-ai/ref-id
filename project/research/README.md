# Research

Investigations that feed a decision here — a comparison, a spike, a measurement. Input to a plan or an ADR, never a record of what was decided.

| Research | Conclusion | Feeds |
|---|---|---|
| [Does the scheme identify what real producers declare, and what they have already stored?](the-scheme-against-real-corpora.md) | It covers what a manifest proves: over nine in ten declared things map with every part taken from a declaration, and admitting a moment (`when`) turns stored readings into distinct units; everything left is a name no manifest declares, plus one gap in Package URL (no type for marketplace extensions). Confidence **high** | [Plan-001](../plans/001-the-identity-package.md) Tracks 3–4; the `state`/`when`, manifest-corpus and typed-fragment rules of `spec/ref-id.json` |
